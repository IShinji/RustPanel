# RustPanel 任务清单 v3.0 - 与宝塔免费版功能对齐

更新日期：2026-05-09

## 版本目标

补齐 RustPanel 在**可视化安全、进阶站点、自动化运维、监控历史**方面与宝塔免费版的代差。实现真正“开箱即用”且具备“生产防御能力”的运维面板。

## 对标参考：宝塔面板核心功能概览

以下是作为对标参考的宝塔面板核心功能总结：

### 1. 状态监控与历史回溯
* **多维度指标可视化**：以折线图展示平均负载（1/5/15分钟）、CPU 使用率、内存占用、磁盘 I/O 及网络 I/O。
* **时间维度筛选**：支持按今天、昨天、最近 7 天及自定义时间段查询历史数据，方便追溯异常峰值。
* **数据生命周期管理**：支持监控开关、设置数据保存天数（如 30 天）、查看日志占用空间并提供一键清理功能。
* **日报系统**：提供系统运行每日汇总报告，包含安全风险与资源峰值摘要。

### 2. 网站与数据库管理
* **站点管理**：支持域名绑定、目录设置、伪静态配置等。
* **数据库管理**：支持 MySQL, SQLServer, PgSQL, SQLite, MongoDB, Redis 的一站式管理。包含环境一键部署、远程连接、自动备份、数据库同步及批量操作。
* **可视化工具**：内置 phpMyAdmin 跳转入口，支持可视化 SQL 操作与表结构修改。
* **反向代理管理**：支持为 Node.js, Go, Python 等项目创建转发规则，支持列表展示、状态控制及 SSL 实时监控。
* **SSL 证书管理**：支持一键申请（Let's Encrypt/免费/商用）、手动导入及分组管理。提供证书过期实时预警、到期天数统一视图，以及核心的“自动续签与静默部署”功能。
* **多项目管理**：支持按 PHP、Java、Node、Go、Python、.Net 及 HTML 进行分类管理。

### 3. 系统安全与防御
* **可视化防火墙**：支持端口放行/屏蔽（TCP/UDP）、来源 IP 限制、禁 Ping 设置及端口防扫描。提供防火墙总开关及规则导入导出。
* **WAF (Nginx 防火墙)**：提供抗 CC 攻击、漏洞入侵防护、恶意扫描拦截及关键词过滤。支持攻击总数统计（含全球攻击地图）、趋势分析及站点级差异化配置。
* **SSH 安全管理**：支持修改默认 22 端口、一键开启/关闭 SSH 服务。
* **多维度防护**：包含恶意 IP 自动封禁、地区访问限制（国家/地区屏蔽）、端口转发管理。
* **进阶加固**：集成入侵防御（文件篡改监控）、扫描感知及网站违规词审计。

### 4. 软件与环境部署
* **应用商店**：一键安装 Nginx、Apache、PHP、Node.js 等开发环境及各类工具插件。
* **Docker 容器管理**：图形化管理容器生命周期（启停、重启、暂停、删除、批量操作）。提供容器内 Web 终端、实时日志、镜像管理（拉取/构建）、Docker Compose 编排、虚拟网络及存储卷管理。支持一键清理冗余资源及容器安全扫描。

### 5. 系统工具
* **图形化文件管理**：支持 Web 端文件增删改查、批量操作、拖拽上传及大文件下载。包含面包屑导航与常用路径收藏功能。
* **开发者利器**：集成在线代码编辑器、目录级 Web 终端快捷入口及跨文件内容搜索。
* **安全与审计**：支持图形化修改权限/所有者、文件回收站（防误删）及核心文件防篡改监控。
* **日志审计中心**：全面记录面板操作、登录、运行及计划任务日志。支持按关键词搜索、日志一键清理、异地导出归档及基于 AI 的异常行为自动化解读。
* **计划任务**：设置定时备份、定期重启或执行自定义脚本。
* **Web 终端 (xterm)**：全功能 Web SSH 客户端，支持 root 身份登录、多窗口选项卡切换。包含多服务器集中管理（连接列表）、常用命令快捷库，以及高级安全审计（操作录像回溯）。

### 6. 域名与资产管理
* **域名全生命周期**：支持域名注册（一键购号）、实名认证管理及 DNS 解析托管。
* **合规性指引**：内置备案时间节点提示与备案指引，降低合规风险。
* **资产同步**：支持云端账号绑定，实现域名资产与面板的深度集成与同步。

---

**参考环境与页面概览：**
* **操作系统**：Debian 13 企业版
* **面板版本**：v11.7.0
* **域名资产**：计划管理 `19891002.xyz` 相关后缀，当前处于未绑定账号状态。
* **SSL 状态**：当前列表暂无数据，无即将到期或已过期证书记录。
* **当前路径**：`/www/wwwroot/supercard` (SuperCard 项目根目录)。
* **终端状态**：已以 root 身份登录本地服务器 (`racknerd-eb4230a`)，OS 为 Debian GNU/Linux。
* **项目环境**：Docker 部署，Bun/Node.js 运行环境，集成大量 AI 协作配置文件（`.claude`, `GEMINI.md` 等）。
* **操作动态**：近期维护活动频繁（含 SuperCard 资产修复、青龙面板容器操作及大量终端调试行为）。
* **反代项目**：当前运行 2 个（分别指向内部 18081 和 5700 端口）。
* **核心组件**：集成 Nginx 管理、网站流量统计及 AI 助手。
* **数据库状态**：当前未安装 MySQL 环境，提供环境一键部署与同步工具。
* **Docker 状态**：当前运行 8 个容器（包含 supercard 项目后端、数据库、nginx-lb、x-ui 等）。
* **监控状态**：监控已开启，今日凌晨（00:30 左右）出现过资源占用峰值，数据保留 30 天。
* **防火墙状态**：当前生效 30 条端口规则（含 80/443, SSH, 业务端口），大部分规则支持 IPv4/IPv6 双栈。
* **WAF 状态**：处于活跃监控状态，已拦截约 9000 次攻击，系统正实时监测全球请求。

## 执行规则

- 按 `blocker → high → medium → low` 执行。
- 完成后将 `[ ]` 改为 `[x]`。
- 阻塞任务保持 `[ ]` 并添加 `@status: BLOCKED`。

---

## P1 第一步：安全底座与防火墙 (Security & Firewall)

目标：可视化接管宿主机网络安全，保护面板不被暴力破解。

### P1-01 系统防火墙 UI 化
- [ ] **P1-01-1** 定义 `security.proto`：包含端口放行/封禁规则、协议选择（TCP/UDP）、备注说明。
- [ ] **P1-01-2** 后端实现适配层：兼容 `ufw` (Ubuntu/Debian) 和 `firewalld` (CentOS/RHEL)。
- [ ] **P1-01-3** 前端开发“安全管理”页面：展示当前规则列表，支持增删改规则。

### P1-02 SSH 安全增强
- [ ] **P1-02-1** 实现 SSH 端口修改 RPC 及后端逻辑（需同步修改 `/etc/ssh/sshd_config` 并重启服务）。
- [ ] **P1-02-2** 实现 SSH 登录审计：记录并展示最近 100 条登录成功/失败的记录。
- [ ] **P1-02-3** 实现 SSH 密钥管理：在线生成 RSA/Ed25510 密钥对，支持禁用密码登录。

---

## P2 第二步：进阶 Web 运维 (Advanced Web Server)

目标：精细化 Nginx 配置，满足复杂建站需求。

### P2-01 伪静态与重定向
- [ ] **P2-01-1** 扩展 `site.proto`，支持 URL 重写 (Rewrite) 和 HTTP 重定向 (Redirect) 的配置协议。
- [ ] **P2-01-2** 内置常用伪静态模板库：WordPress, Laravel, ThinkPHP, Discuz! 等。
- [ ] **P2-01-3** 前端实现“伪静态”编辑器：支持模板选择和手动输入 Nginx 指令。

### P2-02 进阶代理与 SSL 闭环
- [ ] **P2-02-1** 实现站点级 IP 黑白名单配置。
- [ ] **P2-02-2** 实现子目录反向代理配置界面。
- [ ] **P2-02-3** 完善 SSL 自动续期：后台定时检测证书有效期（< 30天），自动触发 `acme-lib` 续签流程并重载 Nginx。

---

## P3 第三步：文件管理与运维工具 (Files & Utilities)

目标：提升日常文件处理效率，降低误操作风险。

### P3-01 远程下载与回收站
- [ ] **P3-01-1** 实现“远程下载”功能：输入 URL 直接将文件下载到指定目录，后端显示下载进度。
- [ ] **P3-01-2** 实现“回收站”功能：删除文件时移动至 `.rustpanel_trash` 目录并保留原路径信息。
- [ ] **P3-01-3** 前端开发“回收站”管理模块：支持一键清空或按需还原。

### P3-02 全局搜索与替换
- [ ] **P3-02-1** 利用 `ripgrep` (如果环境有) 或后端原生实现目录级的内容搜索。
- [ ] **P3-02-2** 前端编辑器支持全局内容搜索与批量替换。

---

## P4 第四步：监控历史与性能看板 (Monitoring History)

目标：不仅看到当下，更能溯源异常。

### P4-01 时序数据持久化
- [ ] **P4-01-1** 引入 SQLite 或嵌入式时序存储，记录每分钟的 CPU、内存、IO、网络指标。
- [ ] **P4-01-2** 实现数据自动滚动清理机制：默认保留最近 30 天的历史数据。
- [ ] **P4-01-3** 前端 Dashboard 增加“历史趋势”图表：支持查看 1h, 24h, 7d 的统计曲线。

---

## P5 第五步：自动化维护任务 (Auto-Maintenance)

目标：将重复劳动交给系统。

### P5-01 任务模板扩展
- [ ] **P5-01-1** 增加“日志切割/清理”任务模板：自动清理 Nginx 日志、系统临时文件。
- [ ] **P5-01-2** 增加“URL 监控任务”：定时访问特定 URL，记录响应状态码。
- [ ] **P5-01-3** 增加“站点/数据库自动备份”至异地（S3/R2/OSS）的完整流程闭环。

---

## 验证计划

- [ ] **V-01** `./scripts/verify-all.sh` 通过。
- [ ] **V-02** 防火墙规则验证：放行 8888 端口后，从外部确认可访问。
- [ ] **V-03** 伪静态验证：创建 WordPress 站点并应用模板，确认固定链接正常。
- [ ] **V-04** 回收站验证：删除文件后在回收站成功还原，且权限无变化。
