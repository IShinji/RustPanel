# RustPanel 任务清单 vX.Y

更新日期：YYYY-MM-DD

## 版本目标

定义本版本可交付结果。

## 执行规则

- 按 `blocker → high → medium → low` 执行。
- 完成后将 `[ ]` 改为 `[x]`。
- 阻塞任务保持 `[ ]` 并添加 `@status: BLOCKED`。

## P1 基础交付

目标：完成当前版本必须交付的功能。

### P1-01 功能域

- [ ] **P1-01-1** 任务标题
  - 优先级：high
  - 验收标准：
    - 可观察行为。
    - 本地验证命令通过。

## 验证计划

- [ ] **V-01** `./scripts/verify-all.sh`

## 后续拓展池

- 暂不纳入当前版本的想法。
